/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "cdn-icons-png.flaticon.com",
          "upload.wikimedia.org"
        ],
      },
};

export default nextConfig;
